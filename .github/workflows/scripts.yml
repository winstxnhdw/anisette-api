name: Scripts

on:
  push:
    branches: main
    paths:
      - .github/workflows/scripts.yml
      - tsconfig.json
      - package.json
      - bun.lockb
      - scripts/*

jobs:
  test:
    uses: winstxnhdw/actions/.github/workflows/bun.yml@main
    with:
      disable-test: true
      disable-build: true
